crowy_declare_module(Platform)

target_link_libraries(CrowyPlatform
PUBLIC
    Crowy::CommonRHI
PRIVATE
    SDL3::SDL3
    Crowy::Core
    Crowy::Input
    Crowy::Log
    Crowy::RHI
)

if(APPLE)
    target_link_libraries(CrowyPlatform
    PRIVATE
        Crowy::MetalRHI
    )
elseif(WIN32)
    target_link_libraries(CrowyPlatform
    PRIVATE
        Crowy::D3D12RHI
    )
else()
    target_link_libraries(CrowyPlatform
    PRIVATE
        Crowy::NullRHI
    )
endif()
